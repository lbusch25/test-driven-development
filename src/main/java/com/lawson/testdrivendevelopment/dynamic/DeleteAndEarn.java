package com.lawson.testdrivendevelopment.dynamic;

public class DeleteAndEarn {

    // So we need to solve each round
    // two state variables? 1) element to choose 2) elements remaining in the array
    // base case - 1 = 1 point
    public static int deleteAndEarn(int[] nums) {
        return 0;
    }
}
